# Python files
*.pyc
__pycache__/

# Django stuff:
db.sqlite3
*.sqlite3*
media/
static/
# Autogenerated stuff:
logs/

# document binary files
*.pdf
*.docx

# Virtual environment:
env/
venv/
.venv/

# Node / SCSS build deps (only the compiled theme.css is committed; see myocyte/scss/theme.scss)
node_modules/

# misc
.DS_Store
.env
models_diagram.png
/*.png
/*/*.png 
!/*/*/*/*.png
.vscode

# Files for VHP4Safety instant
docker-compose-vhp4safety.yml
.env-vhp4safety
docker-compose-MacOs.yml

# Analysis files
data_analysis/

# temp file
change_toxtemp.md

# testings
.coverage
.coverage.*
*.coveragerc

# pytest cache
.pytest_cache/
# Ruff cache
.ruff_cache/ 

# Queue storage
__queuestorage__/

# evaluation outputs
output/

#backups
backups/
.claude/

# Local archives (e.g. macOS "Compress" snapshots of evaluation folders). These
# bundle the ignored input_files/ and output/ data, so git must never see them.
*.zip

# Real-world evaluation input documents (unpublished data).
# Ignore every document but keep the folder structure tracked via .gitkeep files.
myocyte/toxtempass/evaluation/real_world/input_files/**
!myocyte/toxtempass/evaluation/real_world/input_files/**/
!myocyte/toxtempass/evaluation/real_world/input_files/**/.gitkeep

# gold_standard outputs (gold answers + reviewer emails = PII): ignore the data,
# keep the folder tracked via .gitkeep.
!myocyte/toxtempass/evaluation/gold_standard/output/
myocyte/toxtempass/evaluation/gold_standard/output/**
!myocyte/toxtempass/evaluation/gold_standard/output/.gitkeep
